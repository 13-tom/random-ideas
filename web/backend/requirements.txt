fastapi>=0.115
uvicorn[standard]>=0.32
sqlmodel>=0.0.22
pydantic-settings>=2.6
pyjwt[crypto]>=2.9
boto3>=1.35
httpx>=0.27
yt-dlp>=2026.7.4

# Postgres driver SQLAlchemy needs to actually connect - was only ever
# installed manually in local testing venvs, never added here until a real
# Render deploy failed on it (ModuleNotFoundError: No module named
# 'psycopg2'). -binary (not plain psycopg2) so it doesn't need libpq-dev
# compiled into the Docker image.
psycopg2-binary>=2.9

# Only needed if queue_backend=cloud_tasks (see app/config.py); the
# default "local" backend doesn't import this.
# google-cloud-tasks>=2.16
